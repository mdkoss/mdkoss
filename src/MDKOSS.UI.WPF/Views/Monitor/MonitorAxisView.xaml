<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Monitor.MonitorAxisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="轴调试 →" Command="{Binding GoToolCommand}" CommandParameter="debug_axis" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="轴状态监控" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="只读" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="全轴一览 · 选中查看 Faceplate" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource ToolNoteStyle}">
      <TextBlock Text="默认显示全部轴。点动 / 使能 / 定位请进入轴调试。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <WrapPanel DockPanel.Dock="Top">
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="轴数量" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding KpiTotal}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="在线" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding KpiOnline}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="使能" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding KpiEnabled}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="报警位" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding KpiAlarm}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
    </WrapPanel>
    <TextBox DockPanel.Dock="Top" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
             Margin="0,0,0,10" Width="240" HorizontalAlignment="Left"/>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="1.2*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Row="0">
        <DataGrid ItemsSource="{Binding Axes}" SelectedItem="{Binding Selected, Mode=TwoWay}">
          <DataGrid.Columns>
            <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
            <DataGridTextColumn Header="在线" Binding="{Binding Online}" Width="70"/>
            <DataGridTextColumn Header="使能" Binding="{Binding Enabled}" Width="70"/>
            <DataGridTextColumn Header="指令位" Binding="{Binding Prf}" Width="80"/>
            <DataGridTextColumn Header="编码位" Binding="{Binding Enc}" Width="80"/>
            <DataGridTextColumn Header="速度" Binding="{Binding Vel}" Width="70"/>
            <DataGridTextColumn Header="状态位" Binding="{Binding Flags}" Width="1.6*"/>
          </DataGrid.Columns>
        </DataGrid>
      </Border>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Row="1">
        <StackPanel>
          <TextBlock Text="{Binding FpTitle, StringFormat=Faceplate · {0}}" FontWeight="SemiBold" Margin="0,0,0,8"/>
          <WrapPanel>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="状态" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpState}" FontSize="16" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="驱动" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpDriver}" FontSize="16" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="在线" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpOnline}" FontSize="16" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="使能" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpEnabled}" Style="{StaticResource KpiValueStyle}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="指令位置" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpPrf}" Style="{StaticResource KpiValueStyle}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="编码器" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpEnc}" Style="{StaticResource KpiValueStyle}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="速度" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpVel}" Style="{StaticResource KpiValueStyle}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiCardStyle}"><StackPanel><TextBlock Text="Status Word" Style="{StaticResource KpiLabelStyle}"/><TextBlock Text="{Binding FpRaw}" FontSize="16" FontWeight="Bold"/></StackPanel></Border>
          </WrapPanel>
          <TextBlock Text="{Binding FpFlags, StringFormat=轴状态位：{0}}" Foreground="{DynamicResource ToolMutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>
      </Border>
    </Grid>
  </DockPanel>
</UserControl>
