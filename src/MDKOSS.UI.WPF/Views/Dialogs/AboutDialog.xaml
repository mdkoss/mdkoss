<UserControl x:Class="MDKOSS.UI.WPF.Views.Dialogs.AboutDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
  <DockPanel Margin="16">
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
      <Button DockPanel.Dock="Right" Content="关闭" Command="{Binding CloseCommand}" Style="{StaticResource DialogButtonStyle}"/>
      <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
    </DockPanel>
    <ScrollViewer>
      <StackPanel>
        <DataGrid ItemsSource="{Binding Info}" Height="180" Margin="0,0,0,12">
          <DataGrid.Columns>
            <DataGridTextColumn Header="项" Binding="{Binding Key}" Width="1*"/>
            <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="2*"/>
          </DataGrid.Columns>
        </DataGrid>
        <TextBlock Text="监控" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,6"/>
        <ItemsControl ItemsSource="{Binding MonitorLinks}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Button Content="{Binding Label}" Command="{Binding DataContext.OpenToolCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      CommandParameter="{Binding}" Style="{StaticResource DialogButtonStyle}" Margin="0,0,8,8"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="调试" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,6"/>
        <ItemsControl ItemsSource="{Binding DebugLinks}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Button Content="{Binding Label}" Command="{Binding DataContext.OpenToolCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      CommandParameter="{Binding}" Style="{StaticResource DialogButtonStyle}" Margin="0,0,8,8"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="配置" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,6"/>
        <ItemsControl ItemsSource="{Binding ManLinks}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Button Content="{Binding Label}" Command="{Binding DataContext.OpenToolCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      CommandParameter="{Binding}" Style="{StaticResource DialogButtonStyle}" Margin="0,0,8,8"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>
    </ScrollViewer>
  </DockPanel>
</UserControl>
