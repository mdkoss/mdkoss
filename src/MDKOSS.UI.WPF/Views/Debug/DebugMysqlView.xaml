<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugMysqlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="MySQL 调试" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="连接 / 断开 / Query" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="Query 无需确认；断开需确认。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
      <TextBlock Text="设备" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding Selected}" DisplayMemberPath="Name" Width="280"/>
    </DockPanel>
        <TextBox DockPanel.Dock="Top" Text="{Binding Sql, UpdateSourceTrigger=PropertyChanged}"
             AcceptsReturn="True" Height="80" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
      <Button Content="连接" Command="{Binding ConnectCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="断开" Command="{Binding DisconnectCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="Query" Command="{Binding QueryCommand}" Style="{StaticResource ToolBtnStyle}"/>
    </StackPanel>
  </DockPanel>
</UserControl>
