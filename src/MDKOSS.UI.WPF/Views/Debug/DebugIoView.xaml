<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugIoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="只读监控 →" Command="{Binding GoToolCommand}" CommandParameter="monitor_io" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="IO 强制输出" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="DO / VIO 点击拨动（写入确认）" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="强制输出会影响现场执行器。写入前请确认安全联锁。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Column="0" Margin="0,0,8,0">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="数字输入 DI" FontWeight="SemiBold" Margin="0,0,0,8"/>
          <DataGrid ItemsSource="{Binding Inputs}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="设备" Binding="{Binding DeviceName}" Width="*"/>
              <DataGridTextColumn Header="别名" Binding="{Binding Alias}" Width="*"/>
              <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="*"/>
              <DataGridTextColumn Header="在线" Binding="{Binding Online}" Width="70"/>
              <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="60"/>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Column="1">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="数字输出 DO · 点击拨动" FontWeight="SemiBold" Margin="0,0,0,8"/>
          <DataGrid ItemsSource="{Binding Outputs}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="设备" Binding="{Binding DeviceName}" Width="*"/>
              <DataGridTextColumn Header="别名" Binding="{Binding Alias}" Width="*"/>
              <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="*"/>
              <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="60"/>
              <DataGridTemplateColumn Header="" Width="80">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Content="拨动" Style="{StaticResource ToolBtnStyle}"
                            Command="{Binding DataContext.ToggleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"/>
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>
    </Grid>
  </DockPanel>
</UserControl>
