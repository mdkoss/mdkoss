<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugMachineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <Button DockPanel.Dock="Right" Content="主界面" Command="{Binding GoToolCommand}" CommandParameter="" Style="{StaticResource ToolLinkStyle}"
              Visibility="Collapsed"/>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="整机手动" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="启停复位 · 运行摘要 · 快捷跳转" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}" FontSize="12">
        调试模式可写。生产启停优先使用主界面；此处用于联调与维护。启动 / 停止 / 复位需确认。
        <Hyperlink Command="{Binding GoToolCommand}" CommandParameter="monitor_runtime" Foreground="{DynamicResource ToolAccentBrush}" FontWeight="Bold">运行总览（只读）</Hyperlink>
      </TextBlock>
    </Border>
    <UniformGrid DockPanel.Dock="Top" Columns="4" Margin="0,0,0,10">
      <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="项目" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding Project}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="运行时" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding RuntimeOn}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="整机状态" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding Machine}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="操作任务" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding Op}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
    </UniformGrid>
    <Border Style="{StaticResource ToolCardStyle}" DockPanel.Dock="Top">
      <StackPanel>
        <TextBlock Text="任务操作" Style="{StaticResource ToolPanelTitleStyle}"/>
        <StackPanel Orientation="Horizontal">
          <Button Content="启动" Command="{Binding StartCommand}" Style="{StaticResource ToolBtnPrimaryStyle}"/>
          <Button Content="暂停" Command="{Binding PauseCommand}" Style="{StaticResource ToolBtnStyle}"/>
          <Button Content="停止" Command="{Binding StopCommand}" Style="{StaticResource ToolBtnDangerStyle}"/>
          <Button Content="复位" Command="{Binding ResetCommand}" Style="{StaticResource ToolBtnStyle}"/>
        </StackPanel>
      </StackPanel>
    </Border>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Column="0" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="故障摘要" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding Fault}" FontSize="16" FontWeight="Bold" Margin="0,8,0,0" Foreground="{DynamicResource ToolTextBrush}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource ToolCardStyle}" Grid.Column="1">
        <StackPanel>
          <TextBlock Text="调试入口" Style="{StaticResource KpiLabelStyle}"/>
          <WrapPanel Margin="0,8,0,0">
            <Button Content="平台示教" Command="{Binding GoToolCommand}" CommandParameter="debug_platform" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="轴" Command="{Binding GoToolCommand}" CommandParameter="debug_axis" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="IO 强制" Command="{Binding GoToolCommand}" CommandParameter="debug_io" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="串口" Command="{Binding GoToolCommand}" CommandParameter="debug_serial" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="相机" Command="{Binding GoToolCommand}" CommandParameter="debug_camera" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="视觉" Command="{Binding GoToolCommand}" CommandParameter="debug_vision" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="报警" Command="{Binding GoToolCommand}" CommandParameter="debug_alarm" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
            <Button Content="驱动" Command="{Binding GoToolCommand}" CommandParameter="debug_driver" Style="{StaticResource ToolLinkStyle}" Margin="0,0,8,6"/>
          </WrapPanel>
        </StackPanel>
      </Border>
    </Grid>
  </DockPanel>
</UserControl>
