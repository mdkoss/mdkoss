<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugAlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="报警监控 →" Command="{Binding GoToolCommand}" CommandParameter="monitor_alarm" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="报警调试" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="确认 / 复位 / 模拟触发" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="确认、复位锁存、模拟触发需确认。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <WrapPanel DockPanel.Dock="Top">
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="活动" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding VActive}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="作业" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding VOp}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
    </WrapPanel>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <Button Content="全部确认" Command="{Binding AckAllCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="复位锁存" Command="{Binding ResetCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="模拟触发" Command="{Binding TestCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="任务+报警复位" Command="{Binding TaskResetCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="绿灯" Command="{Binding LampCommand}" CommandParameter="green" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="黄灯" Command="{Binding LampCommand}" CommandParameter="yellow" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="红灯" Command="{Binding LampCommand}" CommandParameter="red" Style="{StaticResource ToolBtnStyle}"/>
    </StackPanel>
    <Border Style="{StaticResource ToolCardStyle}">
      <DataGrid ItemsSource="{Binding Items}">
        <DataGrid.Columns>
          <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="70"/>
          <DataGridTextColumn Header="代码" Binding="{Binding Code}" Width="90"/>
          <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
          <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="1.8*"/>
          <DataGridTextColumn Header="时间" Binding="{Binding TriggerTime}" Width="140"/>
          <DataGridTemplateColumn Header="" Width="80">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Button Content="清除" Style="{StaticResource ToolBtnStyle}"
                        Command="{Binding DataContext.ClearOneCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </DockPanel>
</UserControl>
