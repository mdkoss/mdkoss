<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugVisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="视觉监控 →" Command="{Binding GoToolCommand}" CommandParameter="monitor_vision" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="视觉调试" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="run / captureAndRun" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="视觉 run / captureAndRun 需确认。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
      <TextBlock Text="设备" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding Selected}" DisplayMemberPath="Name" Width="280"/>
    </DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Result}" Margin="0,0,0,10" FontSize="16"/>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
      <Button Content="Run" Command="{Binding RunCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="CaptureAndRun" Command="{Binding CaptureRunCommand}" Style="{StaticResource ToolBtnStyle}"/>
    </StackPanel>
  </DockPanel>
</UserControl>
