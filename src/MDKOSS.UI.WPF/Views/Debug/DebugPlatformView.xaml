<UserControl x:Class="MDKOSS.UI.WPF.Views.Tools.Debug.DebugPlatformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="只读监控 →" Command="{Binding GoToolCommand}" CommandParameter="monitor_platform" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="平台示教" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="平台步进 / 点动" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="点动与步进无需确认；平台使能 / 去使能需确认。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
        <Button Content="使能" Command="{Binding EnableCommand}" Style="{StaticResource ToolBtnStyle}"/>
        <Button Content="去使能" Command="{Binding DisableCommand}" Style="{StaticResource ToolBtnStyle}"/>
        <Button Content="停止" Command="{Binding StopCommand}" Style="{StaticResource ToolBtnStyle}"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="平台" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <ComboBox ItemsSource="{Binding Platforms}" SelectedItem="{Binding Selected}" DisplayMemberPath="Name" Width="240"/>
        <TextBlock Text="速度" VerticalAlignment="Center" Margin="16,0,8,0"/>
        <TextBox Text="{Binding Velocity, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
        <TextBlock Text="步进" VerticalAlignment="Center" Margin="16,0,8,0"/>
        <TextBox Text="{Binding Step, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
      </StackPanel>
    </DockPanel>
    <Border Style="{StaticResource ToolCardStyle}" DockPanel.Dock="Top">
      <ItemsControl ItemsSource="{Binding Axes}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <DockPanel Margin="0,0,0,8">
              <TextBlock Text="{Binding Letter, StringFormat=轴 {0}}" Width="56" VerticalAlignment="Center" FontWeight="SemiBold"/>
              <TextBlock Text="{Binding Prf, StringFormat=pos {0}}" Width="100" VerticalAlignment="Center"/>
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <RepeatButton Content="Jog −" Command="{Binding DataContext.JogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding Letter, StringFormat={}{0}-}" Style="{StaticResource JogButtonStyle}" Delay="200" Interval="80"/>
                <RepeatButton Content="Jog +" Command="{Binding DataContext.JogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding Letter, StringFormat={}{0}+}" Style="{StaticResource JogButtonStyle}" Delay="200" Interval="80"/>
                <Button Content="步进 −" Command="{Binding DataContext.StepCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding Letter, StringFormat={}{0}-}" Style="{StaticResource ToolBtnStyle}"/>
                <Button Content="步进 +" Command="{Binding DataContext.StepCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding Letter, StringFormat={}{0}+}" Style="{StaticResource ToolBtnStyle}"/>
              </StackPanel>
            </DockPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Border>
    <Border Style="{StaticResource ToolCardStyle}">
      <DataGrid ItemsSource="{Binding Axes}">
        <DataGrid.Columns>
          <DataGridTextColumn Header="轴" Binding="{Binding Letter}" Width="50"/>
          <DataGridTextColumn Header="轴设备" Binding="{Binding AxisId}" Width="*"/>
          <DataGridTextColumn Header="在线" Binding="{Binding Online}" Width="70"/>
          <DataGridTextColumn Header="使能" Binding="{Binding Enabled}" Width="70"/>
          <DataGridTextColumn Header="指令" Binding="{Binding Prf}" Width="80"/>
          <DataGridTextColumn Header="编码" Binding="{Binding Enc}" Width="80"/>
          <DataGridTextColumn Header="速度" Binding="{Binding Vel}" Width="70"/>
          <DataGridTextColumn Header="状态位" Binding="{Binding Flags}" Width="1.4*"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </DockPanel>
</UserControl>
