<UserControl x:Class="MDKOSS.UI.WPF.Sample.Views.MonitorSampleExtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Top">
        <Button Content="联调 →" Command="{Binding GoToolCommand}" CommandParameter="debug_sampleext" Style="{StaticResource ToolLinkStyle}"/>
      </StackPanel>
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="SampleExt 监控" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="只读" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="自定义设备 samplebeacon · MotionTask samplemotion" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource ToolNoteStyle}">
      <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}" FontSize="12">
        本页只读。脉冲与运动命令请到
        <Hyperlink Command="{Binding GoToolCommand}" CommandParameter="debug_sampleext" Foreground="{DynamicResource ToolAccentBrush}" FontWeight="Bold">扩展示例联调</Hyperlink>。
      </TextBlock>
    </Border>
    <UniformGrid DockPanel.Dock="Top" Columns="3" Margin="0,0,0,12">
      <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="信标" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding BeaconLabel}" Style="{StaticResource KpiValueStyle}" FontSize="16"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,8,0">
        <StackPanel>
          <TextBlock Text="脉冲次数" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding PulseCount}" Style="{StaticResource KpiValueStyle}"/>
        </StackPanel>
      </Border>
      <Border Style="{StaticResource KpiCardStyle}">
        <StackPanel>
          <TextBlock Text="运动阶段" Style="{StaticResource KpiLabelStyle}"/>
          <TextBlock Text="{Binding MotionPhase}" Style="{StaticResource KpiValueStyle}" FontSize="16"/>
        </StackPanel>
      </Border>
    </UniformGrid>
    <StackPanel>
      <TextBlock Text="{Binding BeaconMessage, StringFormat=信标：{0}}" Foreground="{DynamicResource ToolMutedBrush}" Margin="0,0,0,6"/>
      <TextBlock Text="{Binding MotionMessage, StringFormat=运动：{0}}" Foreground="{DynamicResource ToolMutedBrush}" Margin="0,0,0,6"/>
      <TextBlock Text="{Binding MotionCycles, StringFormat=循环次数：{0}}" Foreground="{DynamicResource ToolMutedBrush}"/>
    </StackPanel>
  </DockPanel>
</UserControl>
