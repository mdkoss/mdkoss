<UserControl x:Class="MDKOSS.UI.WPF.Sample.Views.DebugModbusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource ToolBgBrush}">
  <DockPanel Margin="20,12">
    <TextBlock DockPanel.Dock="Bottom" Style="{StaticResource ToolStatusStyle}" Text="{Binding Status}"/>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Modbus 调试" Style="{StaticResource ToolTitleStyle}"/>
          <Border Style="{StaticResource ToolPillStyle}"><TextBlock Text="可写" Style="{StaticResource ToolPillTextStyle}"/></Border>
        </StackPanel>
        <TextBlock Text="devmodserver / devmodclient · Holding 读写" Style="{StaticResource ToolSubStyle}"/>
      </StackPanel>
    </DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource WritableNoteStyle}">
      <TextBlock Text="读无需确认；停止/断开与写 Holding 需确认。" TextWrapping="Wrap" Foreground="{DynamicResource ToolMutedBrush}"/>
    </Border>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
      <TextBlock Text="设备" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding Selected}" DisplayMemberPath="Name" Width="280"/>
      <TextBlock Text="{Binding Role, StringFormat=  角色：{0}}" VerticalAlignment="Center" Margin="12,0,0,0"
                 Foreground="{DynamicResource ToolMutedBrush}"/>
    </DockPanel>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock Text="地址" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Width="72" Margin="0,0,12,0"/>
      <TextBlock Text="数量" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox Text="{Binding Count, UpdateSourceTrigger=PropertyChanged}" Width="72" Margin="0,0,12,0"/>
      <TextBlock Text="写入值" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox Text="{Binding Values, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
    </StackPanel>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <Button Content="启动/连接" Command="{Binding StartCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="停止/断开" Command="{Binding StopCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="读 Holding" Command="{Binding ReadCommand}" Style="{StaticResource ToolBtnStyle}"/>
      <Button Content="写 Holding" Command="{Binding WriteCommand}" Style="{StaticResource ToolBtnStyle}"/>
    </StackPanel>
    <TextBox Text="{Binding Detail, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
             VerticalScrollBarVisibility="Auto" MinHeight="120"/>
  </DockPanel>
</UserControl>
